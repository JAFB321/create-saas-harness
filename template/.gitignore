node_modules/
.next/
.turbo/
dist/
coverage/
*.tsbuildinfo
next-env.d.ts

# env (never commit real keys)
.env
.env.local
.env*.local

# supabase local
supabase/.branches/
supabase/.temp/

# impeccable design skill: live-editing session state (the design system itself —
# .impeccable/design.json, DESIGN.md, PRODUCT.md — IS committed as the UI source of truth)
.impeccable/live/
.impeccable/critique/

# test artifacts
test-results/
playwright-report/

.DS_Store
*.log
