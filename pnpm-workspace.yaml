# Workspace members: the CLI and the landing page.
# `template/` is raw files copied by the CLI — it is intentionally NOT a workspace
# member so pnpm does not try to install or link the template's own packages here.
packages:
  - "packages/*"
  - "landing"
